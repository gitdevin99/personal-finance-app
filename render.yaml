services:
  - type: static
    name: personal-finance-app
    env: static
    buildCommand: mkdir -p dist && cp -r index.html login dashboard signup onboarding assets dist/ && cp static.json render.yaml dist/
    publishDir: dist
